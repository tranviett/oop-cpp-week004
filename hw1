#include <iostream>
using namespace std;

// Bai 1: Tim so lon nhat trong 3 so
void bai1() {
    cout << "\n=== BAI 1: TIM SO LON NHAT TRONG 3 SO ===\n";
    int a, b, c;
    cout << "Nhap a, b, c: ";
    cin >> a >> b >> c;
    int max = a;
    if (b > max) max = b;
    if (c > max) max = c;
    cout << "So lon nhat la: " << max << endl;
}