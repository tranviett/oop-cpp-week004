#include <iostream>
using namespace std;
// Bai 4: Kiem tra so doi xung
void bai4() {
    cout << "\n=== BAI 4: KIEM TRA SO DOI XUNG ===\n";
    int n;
    cout << "Nhap n: ";
    cin >> n;
    int temp = n;
    int daoNguoc = 0;
    while (temp > 0) {
        daoNguoc = daoNguoc * 10 + (temp % 10);
        temp /= 10;
    }
    if (daoNguoc == n) {
        cout << n << " la so doi xung." << endl;
    } else {
        cout << n << " khong phai la so doi xung." << endl;
    }
}